# codeX-backend

### Steps to run

1. Run `pipenv shell` in root dir
2. Do `pipenv install` in root dir
3. Run `python main.py` in root dir

### Tasks

Design API's for the following:

1. ~~Convert Python code into pseudo code // input as file but we require i/p as text~~
2. ~~Run code give output give time space // hackerearth api~~
3. ~~Give code suggestions/warnings/errors using pylint // we can run pylint from terminal, how will we do it from code~~
4. ~~Translate pseudo code in multiple languages // copy paste~~
5. ~~Code sharing with link // after frontend~~
